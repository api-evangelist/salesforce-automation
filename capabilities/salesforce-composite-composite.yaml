naftiko: 1.0.0-alpha2
info:
  label: Salesforce Composite API — Composite
  description: 'Salesforce Composite API — Composite. 1 operations. Lead operation: Execute composite request. Self-contained
    Naftiko capability covering one Salesforce Automation business surface.'
  tags:
  - Salesforce Automation
  - Composite
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SALESFORCE_AUTOMATION_API_KEY: SALESFORCE_AUTOMATION_API_KEY
capability:
  consumes:
  - type: http
    namespace: salesforce-composite-composite
    baseUri: https://{instance}.salesforce.com/services/data/v63.0
    description: Salesforce Composite API — Composite business capability. Self-contained, no shared references.
    resources:
    - name: composite
      path: /composite
      operations:
      - name: executecomposite
        method: POST
        description: Execute composite request
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.SALESFORCE_AUTOMATION_API_KEY}}'
  exposes:
  - type: rest
    namespace: salesforce-composite-composite-rest
    port: 8080
    description: REST adapter for Salesforce Composite API — Composite. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/composite
      name: composite
      description: REST surface for composite.
      operations:
      - method: POST
        name: executecomposite
        description: Execute composite request
        call: salesforce-composite-composite.executecomposite
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: salesforce-composite-composite-mcp
    port: 9090
    transport: http
    description: MCP adapter for Salesforce Composite API — Composite. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: execute-composite-request
      description: Execute composite request
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: salesforce-composite-composite.executecomposite
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
