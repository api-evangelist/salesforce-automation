naftiko: 1.0.0-alpha2
info:
  label: Salesforce Analytics (CRM Analytics) REST API — Lenses
  description: 'Salesforce Analytics (CRM Analytics) REST API — Lenses. 1 operations. Lead operation: List lenses (explorations).
    Self-contained Naftiko capability covering one Salesforce Automation business surface.'
  tags:
  - Salesforce Automation
  - Lenses
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SALESFORCE_AUTOMATION_API_KEY: SALESFORCE_AUTOMATION_API_KEY
capability:
  consumes:
  - type: http
    namespace: salesforce-analytics-lenses
    baseUri: https://{instance}.salesforce.com/services/data/v63.0/wave
    description: Salesforce Analytics (CRM Analytics) REST API — Lenses business capability. Self-contained, no shared references.
    resources:
    - name: lenses
      path: /lenses
      operations:
      - name: getlenses
        method: GET
        description: List lenses (explorations)
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: q
          in: query
          type: string
        - name: folderId
          in: query
          type: string
        - name: page
          in: query
          type: string
        - name: pageSize
          in: query
          type: integer
    authentication:
      type: bearer
      token: '{{env.SALESFORCE_AUTOMATION_API_KEY}}'
  exposes:
  - type: rest
    namespace: salesforce-analytics-lenses-rest
    port: 8080
    description: REST adapter for Salesforce Analytics (CRM Analytics) REST API — Lenses. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/lenses
      name: lenses
      description: REST surface for lenses.
      operations:
      - method: GET
        name: getlenses
        description: List lenses (explorations)
        call: salesforce-analytics-lenses.getlenses
        with:
          q: rest.q
          folderId: rest.folderId
          page: rest.page
          pageSize: rest.pageSize
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: salesforce-analytics-lenses-mcp
    port: 9090
    transport: http
    description: MCP adapter for Salesforce Analytics (CRM Analytics) REST API — Lenses. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: list-lenses-explorations
      description: List lenses (explorations)
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: salesforce-analytics-lenses.getlenses
      with:
        q: tools.q
        folderId: tools.folderId
        page: tools.page
        pageSize: tools.pageSize
      outputParameters:
      - type: object
        mapping: $.
